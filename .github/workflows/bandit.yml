name: Bandit

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: mdegis/bandit-action@v1
        with:
          path: "."
          excluded_paths: "./test/"
